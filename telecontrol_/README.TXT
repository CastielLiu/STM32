2018.9.4  调整初始化顺序，先开启DMA传输，再开启ADC转换，防止ADC转换时因DMA传输未开始导致数据被覆盖，从而引起数据错位。
	增加看门狗  默认开启状态遥控状态。

STM32芯片遥控器 ,发送方

大端模式，中位值为2048
byte0~byte1  左手上下
byte2~byte3  右手上下
byte4~byte5  右手左右
byte6~byte7  左手左右

byte8
{
	bit0  模式
}